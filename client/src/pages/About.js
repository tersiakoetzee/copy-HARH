import { Template } from "../components/Template";
import { AboutContent } from "../components/AboutContent";

export const About = () => (
	<main role="main">
		<Template>
			<AboutContent />
		</Template>
	</main>
);
